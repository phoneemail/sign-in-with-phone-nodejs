doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1, maximum-scale=1")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    meta(name="msapplication-TileColor", content="#0E0E0E")
    meta(name="template-color", content="#0E0E0E")
    meta(name="description", content="Example | NodeJs")
    meta(name="author", content="coder-kabir")
    meta(name="keywords", content="free SMS,OTP,phone email,free sms otp service")
    title Example | Website - Login with Phone | Phone.email

    style
      | .phem-container
      |   display: flex;
      |   align-items: center;
      |   justify-content: center;
      |   margin: 50px 0;
      |
      | .phem-input
      |   padding: 12px 20px;
      |   display: inline-block;
      |   border: 1px solid #ccc;
      |   border-radius: 4px;
      |   box-sizing: border-box;
      |   margin-bottom: 20px;
      |
      | .phem-card
      |   color: #024430 !important;
      |   text-align: center;
      |   background-color: #fff;
      |   padding: 30px;
      |   border-radius: 0.5rem;
      |   box-shadow: 0 1px 3px rgba(17, 24, 39, .09);
      |   width: 100%;
      |   max-width: 420px;
      |   margin: 0 auto;
      |   font-family: sans-serif, serif, system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
      |   line-height: 28px;
      |   display: flex;
      |   flex-direction: column;
      |   align-items: center;
      |   justify-content: center;

  body(style="background-color: #f1f5f9;")
    if hasToken
      .phem-container
        .phem-card
          img.phe-login-img(width="250px", src="https://storage.googleapis.com/prod-phoneemail-prof-images/phem-widgets/phe-signin-success.svg", alt=`Success - You are logged in with ${userDetails.countryCode} ${userDetails.phoneNo}`)
          .phem-card-body
            h1 Welcome!
            h4(style="line-height:36px;") You are logged in successfully with
              | #{userDetails.countryCode}
              | #{userDetails.phoneNo}

          button(style="display: flex; align-items: center; justify-content:center; padding: 14px 20px; background-color: #02BD7E; font-weight: bold; color: #ffffff; border: none; border-radius: 3px; font-size: inherit;cursor:pointer; max-width:320px; width:100%" onclick="window.location.href = '/'") Back

    else
      .phem-container
        .phem-card
          img.phe-login-img(width="250px", src="https://storage.googleapis.com/prod-phoneemail-prof-images/phem-widgets/phe-signin-box.svg", alt="Sign In - Welcome to Sign In with Phone")
          h1(style="margin:10px;") Sign In
          p(style="color:#a6a6a6;") Welcome to Sign In with Phone

          button#btn_ph_login(name="btn_ph_login", type="button", style="display: flex; align-items: center; justify-content:center; padding: 14px 20px; background-color: #02BD7E; font-weight: bold; color: #ffffff; border: none; border-radius: 3px; font-size: inherit;cursor:pointer; max-width:320px; width:100%" onclick="window.open('#{authUrl}', 'peLoginWindow', 'toolbar=0,scrollbars=0,location=0,statusbar=0,menubar=0,resizable=0, width=500, height=560, top=' + (screen.height - 600) / 2 + ', left=' + (screen.width - 500) / 2);")
            img(src="https://storage.googleapis.com/prod-phoneemail-prof-images/phem-widgets/phem-phone.svg", alt="Phone Email - Sign In with Phone" style="margin-right:10px;")
            | Sign In with Phone
